# ETAPA 1
## ULA da Mic-1

Para conseguir executar o programa, é importante estar no diretório de `etapa1.c` e ter o arquivo `entradas.txt` com as entradas para a ULA

Após isso rode no terminal:

```
$ gcc etapa1.c -o etapa1
$ ./etapa1
```
